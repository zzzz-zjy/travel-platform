{
  "buildCommand": "node scripts/vercel-build.js && npx prisma generate && npx prisma db push --force-reset && npx tsx prisma/seed.ts && next build",
  "installCommand": "npm install",
  "framework": "nextjs"
}
